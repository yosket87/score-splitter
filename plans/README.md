# 開発計画

2026-09-04、基準コミット `03ef7db`。

> 001は最新main `bcb51ff` に実装済み。002は同mainを起点とする `feat/payment-status` で実装・検証済み。今後の作業開始時はmainとの差分を再確認する。

利用者は③精算の内訳表示と①精算完了・月締めを採用した。現行画面のデザインを維持し、②未入力管理は実際の運用上ほぼ不要なため対象外とする。

## 順序と状態

| 計画 | 内容 | 優先度 | 工数 | 依存 | 状態 |
|---|---|---|---|---|---|
| [001](001-settlement-breakdown.md) | 精算の内訳表示 | P1 | M | なし | DONE |
| [002](002-month-close-design.md) | 振込前・振込済みの状態表示 | P1 | 実装3〜5日目安 | 001実装済みのmainを起点 | DONE（実装・検証済み） |

002は2026-09-05の要望補足で方針変更。振込済みでも編集を許可し、差額を表示する。改訂仕様で実装・ローカル検証済み。本番配備は未実施。

## 進め方

002は最新main `bcb51ff` の精算内訳を維持し、振込状態・実支払記録・編集後の差額・取消を実装する。月の編集ロックと再開操作は不要。[改訂仕様](002-month-close-spec.md) と [実装計画](002-month-close-implementation.md) を参照。

001は2026-09-04に `feat/settlement-breakdown` で実装・検証済み。全985テスト、Chromium E2E 5件、型検証・Lint・カバレッジ・ビルドが成功し、2サイズ・2テーマの画面を確認した。検証条件・既存警告・スクリーンショット所在は001の検証記録を参照。002は2026-09-05に改訂仕様で実装済み。1,045テスト、全68 E2E、実D1・型・lint・buildの検証を実施。詳細は002実装計画の完了記録を参照。

## 検討して除外・保留した候補

- 未入力管理・定例テンプレート: 利用者から不要との判断。項目名だけ入力して金額を後から埋める状況がほぼない。選択された2機能へ混ぜない。
- コピー元月の選択、CSV取込: 今回は未選択。
- 年間グラフ、パスキー: 既存機能のため新規提案から除外。
- マルチテナント・課金: 現行の需要検証方針を維持し、今回の範囲外。
