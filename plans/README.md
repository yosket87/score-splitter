# 開発計画

2026-09-04、基準コミット `03ef7db`。

> 最新コードとの再照合が必要: 保存時にmainを `38ac423` へ更新した。計画の調査基準から59コミット進み、API構成・テストファイルなどが変更されている。以下の計画は調査時点の提案として保存し、実行前に各計画のドリフト確認を行い、ファイルパス・コマンド・設計上の前提を更新すること。最新mainで実行可能と検証した計画ではない。

利用者は③精算の内訳表示と①精算完了・月締めを採用した。現行画面のデザインを維持し、②未入力管理は実際の運用上ほぼ不要なため対象外とする。

## 順序と状態

| 計画 | 内容 | 優先度 | 工数 | 依存 | 状態 |
|---|---|---|---|---|---|
| [001](001-settlement-breakdown.md) | 精算の内訳表示 | P1 | M | なし | DONE |
| [002](002-month-close-design.md) | 精算完了・月締めの設計スパイク | P1 | 設計M、実装L | 設計は独立、UI実装は001の後を推奨 | TODO |

状態: TODO / IN PROGRESS / DONE / BLOCKED（理由を併記）。002の設計完了は、機能の実装完了を意味しない。

## 進め方

001は現在の計算結果を表示する範囲で単独実装できる。002は既存の月次入力のまま使える精算完了を設計し、端数、実支払記録、再開、サーバー側の書込制限と競合を具体化してから実装へ渡す。新たな未入力状態を前提条件にしない。

001は2026-09-04に `feat/settlement-breakdown` で実装・検証済み。全985テスト、Chromium E2E 5件、型検証・Lint・カバレッジ・ビルドが成功し、2サイズ・2テーマの画面を確認した。検証条件・既存警告・スクリーンショット所在は001の検証記録を参照。002は設計未着手。

## 検討して除外・保留した候補

- 未入力管理・定例テンプレート: 利用者から不要との判断。項目名だけ入力して金額を後から埋める状況がほぼない。選択された2機能へ混ぜない。
- コピー元月の選択、CSV取込: 今回は未選択。
- 年間グラフ、パスキー: 既存機能のため新規提案から除外。
- マルチテナント・課金: 現行の需要検証方針を維持し、今回の範囲外。
